#define _USE_MATH_DEFINES
#include <iostream>
#include <vector>
#include <set>
#include <math.h>

using namespace std;
int main()
{


	set <double> s;
	double a;
	int x, y;
	double pi = M_PI;
	cout.precision(30);
	cin >> a;
	cout << pi * a*a << "\n" << ((2 * a) * (2 * a) )/ 2;
}