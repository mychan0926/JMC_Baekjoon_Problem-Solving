#include <iostream>
#include <vector>
using namespace std;


int main()
{
	int a, b, c, n, m;
	cin >> a >> b;
	if (b == 1||(!(a>=12&&a<=16)))
	{
		cout << 280;
	}
	else
	{
		cout << 320;
	}
}
