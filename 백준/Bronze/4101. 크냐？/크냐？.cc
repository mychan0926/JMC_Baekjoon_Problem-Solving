#include <iostream>

using namespace std;

int main()
{
    while(1)
    {
    int a,b;
    cin >>a>>b;
    if(a>b)
        
    {
        cout <<"Yes"<<"\n";
    }
    else if (a==0&&b==0)
        {
        break;
    }
    else if(b>=a)
    {
        cout <<"No"<<"\n";
    }
    }
}
